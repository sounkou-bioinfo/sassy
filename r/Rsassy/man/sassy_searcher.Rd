% Generated by hand for initial scaffold
\name{sassy_searcher}
\alias{sassy_searcher}
\title{Create a reusable 'sassy' searcher}
\usage{
sassy_searcher(alphabet = c("dna", "iupac", "ascii"), rc = TRUE, alpha = NULL)
}
\arguments{
\item{alphabet}{Alphabet profile. One of \code{"dna"}, \code{"iupac"}, or \code{"ascii"}.}
\item{rc}{If \code{TRUE}, search reverse-complement strand as well where supported.}
\item{alpha}{Optional IUPAC overhang cost in \code{[0, 1]}. Use \code{NULL} to disable.}
}
\value{An external pointer with class \code{sassy_searcher}.}
\description{Create a reusable searcher backed by the Rust 'sassy' crate.}
\examples{
searcher <- sassy_searcher("dna", rc = FALSE)
sassy_searcher_search(searcher, "ACGT", "TTACGTAA", 0)
}
