% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{sassy_search_connection}
\alias{sassy_search_connection}
\title{Search an R connection with 'sassy'}
\usage{
sassy_search_connection(
  pattern,
  con,
  k,
  alphabet = c("dna", "iupac", "ascii"),
  rc = TRUE,
  alpha = NULL,
  all = FALSE,
  chunk_size = 1024 * 1024,
  overlap = NULL
)
}
\arguments{
\item{pattern}{Raw vector or character scalar containing the query pattern.}

\item{con}{An open readable R connection, preferably opened in binary mode.}

\item{k}{Maximum edit distance.}

\item{alphabet}{Alphabet profile. One of \code{"dna"}, \code{"iupac"}, or \code{"ascii"}.}

\item{rc}{If \code{TRUE}, search reverse-complement strand as well where supported.}

\item{alpha}{Optional IUPAC overhang cost in \verb{[0, 1]}. Use \code{NULL} to disable.}

\item{all}{If \code{FALSE}, return local-minimum matches. If \code{TRUE}, return all end positions with score <= \code{k}.}

\item{chunk_size}{Number of new bytes to read per native chunk.}

\item{overlap}{Number of bytes to carry from one chunk to the next. Defaults
to \code{pattern_bytes + k} without overhang, and \code{2 * pattern_bytes + k} when
\code{alpha} is set.}
}
\value{
A data frame of matches with coordinates relative to the full stream.
}
\description{
Streams bytes from an already-open readable R connection through the C/R API
boundary. This avoids an R-level \code{readBin()}/\code{readChar()} loop while still
preserving matches that cross chunk boundaries by keeping an overlap window.
}
